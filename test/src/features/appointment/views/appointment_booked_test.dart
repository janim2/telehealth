import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('AppointmentBookedScreen displays correctly',
      (WidgetTester tester) async {
    // await tester.pumpWidget(
    //   const MaterialApp(
    //     home: AppointmentBookedScreen(),
    //   ),
    // );

    // Verify that the check circle icon is rendered
    // final iconFinder = find.byIcon(Icons.check_circle);
    // expect(iconFinder, findsOneWidget);

    // Verify that the "Congratulations!" text is rendered
    // final congratsTextFinder = find.text('Congratulations!');
    // expect(congratsTextFinder, findsOneWidget);

    // Verify that the success message is rendered
    // final successMessageFinder =
    //     find.text('You have successfully booked an appointment.');
    // expect(successMessageFinder, findsOneWidget);

    // Verify that the MyButton widget is rendered
    // final myButtonFinder = find.byType(MyButton);
    // expect(myButtonFinder, findsOneWidget);

    // Verify that tapping the MyButton triggers context.pushReplacement()
    // await tester.tap(myButtonFinder);
    // final router = GoRouter.of(tester.element(find.byType(AppointmentBookedScreen)));
    // expect(router.currentMatch?.route, '/dashboard');
  });
}
