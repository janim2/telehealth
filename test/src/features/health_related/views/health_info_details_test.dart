import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('HealthInfoDetailsScreen should render correctly',
      (WidgetTester tester) async {
//     await tester.pumpWidget(const MaterialApp(home: HealthInfoDetailsScreen(pageType: 'Test Page')));

//     expect(find.text('Test Page'), findsOneWidget);
//     // expect(find.byType(MyNavbar), findsOneWidget);
//     // Add more expectation checks based on your widget's layout and content
  });

//   // You can write additional tests for different scenarios or behaviors here
}
