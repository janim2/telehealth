import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('MedicationsRemindersScreen displays correctly',
      (WidgetTester tester) async {
    // await tester.pumpWidget(
    //   const MaterialApp(
    //     home: MedicationsRemindersScreen(),
    //   ),
    // );

    // Verify that MyNavbar widget is rendered
    // final myNavbarFinder = find.byType(MyNavbar);
    // expect(myNavbarFinder, findsOneWidget);

    // // Verify that doctor info cards are rendered
    // final doctorInfoCardFinder = find.byType(DoctorInfoCard);
    // expect(doctorInfoCardFinder,
    //     findsNWidgets(3)); // There are three DoctorInfoCard widgets

    // // Verify that the checkbox is rendered
    // final checkboxFinder = find.byType(Checkbox);
    // expect(checkboxFinder, findsOneWidget);

    // // Verify that the "Remind me when it's time" text is rendered
    // final remindMeTextFinder = find.text("Remind me when it's time");
    // expect(remindMeTextFinder, findsOneWidget);

    // Verify that tapping the checkbox toggles its state
    // await tester.tap(checkboxFinder);
    // await tester.pump();
    // expect(tester.state<MedicationsRemindersScreenState>((find.byType(MedicationsRemindersScreen))), (state) => state._isChecked == true);
  });
}
